only-built-dependencies[]=@bufbuild/buf
only-built-dependencies[]=better-sqlite3
only-built-dependencies[]=esbuild

# Ensure @playwright/test's binary wins the .bin/playwright symlink
# over the base playwright package (which has different exit code semantics).
public-hoist-pattern[]=@playwright/test
