{
  "$schema": "https://developer.microsoft.com/json-schemas/rush/v5/command-line.schema.json",
  "commands": [
    {
      "commandKind": "bulk",
      "name": "test",
      "summary": "Run tests for each package",
      "description": "Runs the 'test' script in each project's package.json, if defined.",
      "enableParallelism": true,
      "ignoreMissingScript": true
    }
  ]
}
