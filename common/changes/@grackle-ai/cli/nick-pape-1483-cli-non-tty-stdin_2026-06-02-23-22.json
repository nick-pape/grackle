{
  "changes": [
    {
      "comment": "Fix `grackle token set` and `grackle attach` hanging in non-TTY contexts (CI, pipes, redirects). `token set` now reads the value from stdin when not a TTY (e.g. `echo $SECRET | grackle token set foo`); `attach` skips the readline and streams events, printing a one-shot send-input hint when the session requests input.",
      "type": "patch",
      "packageName": "@grackle-ai/cli"
    }
  ],
  "packageName": "@grackle-ai/cli",
  "email": "5674316+nick-pape@users.noreply.github.com"
}
