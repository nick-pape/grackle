{
  "changes": [
    {
      "comment": "GenUX render-by-source React runtime (#1268): a new `component_show` MCP tool renders agent-authored React/JSX inline in the chat against the Grackle component library, via a single sandboxed runtime resource served from the sandbox origin (new `grackle-react` rendererKind). Adds an `allowUnsafeEval` sandbox-CSP flag (react-live transpile+eval, origin-isolated) and registers `component_show` in the scoped/orchestrator/all MCP tool presets.",
      "type": "minor",
      "packageName": "@grackle-ai/cli"
    }
  ],
  "packageName": "@grackle-ai/cli",
  "email": "5674316+nick-pape@users.noreply.github.com"
}
