syntax = "proto3";

package grackle;

import "grackle/grackle_types.proto";

service GrackleOrchestration {
  // Tasks
  rpc ListTasks(ListTasksRequest) returns (TaskList);
  rpc SearchTasks(SearchTasksRequest) returns (SearchTasksResponse);
  rpc CreateTask(CreateTaskRequest) returns (Task);
  rpc GetTask(TaskId) returns (Task);
  rpc UpdateTask(UpdateTaskRequest) returns (Task);
  rpc StartTask(StartTaskRequest) returns (Session);
  rpc CompleteTask(TaskId) returns (Task);
  rpc ResumeTask(TaskId) returns (Session);
  rpc StopTask(TaskId) returns (Task);
  rpc SetWorkpad(SetWorkpadRequest) returns (Task);
  rpc DeleteTask(TaskId) returns (Empty);

  // Personas
  rpc ListPersonas(Empty) returns (PersonaList);
  rpc CreatePersona(CreatePersonaRequest) returns (Persona);
  rpc GetPersona(PersonaId) returns (Persona);
  rpc UpdatePersona(UpdatePersonaRequest) returns (Persona);
  rpc DeletePersona(PersonaId) returns (Empty);

  // Agents (context-axis standing entities — Phase 0: no lifecycle, #1417)
  rpc ListAgents(Empty) returns (AgentList);
  rpc CreateAgent(CreateAgentRequest) returns (Agent);
  rpc GetAgent(AgentId) returns (Agent);
  rpc UpdateAgent(UpdateAgentRequest) returns (Agent);
  rpc DeleteAgent(AgentId) returns (Empty);
  // Upsert the Agent's heartbeat schedule (#1438). Empty cadence clears it.
  rpc SetAgentHeartbeat(SetAgentHeartbeatRequest) returns (Schedule);

  // Components (agent-authored UI registry)
  rpc RegisterComponent(RegisterComponentRequest) returns (Component);
  rpc UpdateComponent(UpdateComponentRequest) returns (Component);
  rpc GetComponent(GetComponentRequest) returns (Component);
  rpc ListComponents(ListComponentsRequest) returns (ComponentList);
  rpc SearchComponents(SearchComponentsRequest) returns (SearchComponentsResponse);
  rpc SetComponentPromotion(SetComponentPromotionRequest) returns (Component);
  rpc ResolveComponentGraph(ResolveComponentGraphRequest) returns (ResolveComponentGraphResponse);

  // Escalations
  rpc CreateEscalation(CreateEscalationRequest) returns (Escalation);
  rpc ListEscalations(ListEscalationsRequest) returns (EscalationList);
  rpc AcknowledgeEscalation(AcknowledgeEscalationRequest) returns (Escalation);
}
